package backend

import (
	"fmt"
)

func DeserializePropertyCollection(filename string) (PropertyCollection, error) {
	var properties PropertyCollection
	return properties, fmt.Errorf("Not implemented")
}
